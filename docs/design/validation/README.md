# Rendered UI validation

These screenshots are generated by the authenticated Playwright smoke test against a disposable PostgreSQL-backed Filament Manager instance. They document the implemented light, dark, and installed-mobile layout contracts.
