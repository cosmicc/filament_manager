{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Calibration Session",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "filament_product_id",
    "printer_id",
    "nozzle_diameter_mm",
    "status",
    "steps"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "filament_product_id": {
      "type": "string",
      "format": "uuid"
    },
    "printer_id": {
      "type": "string"
    },
    "nozzle_diameter_mm": {
      "type": "number",
      "exclusiveMinimum": 0
    },
    "build_plate_code": {
      "anyOf": [
        {
          "type": "string",
          "pattern": "^P[1-9][0-9]*$",
          "maxLength": 32
        },
        {
          "type": "null"
        }
      ]
    },
    "build_plate_surface_code": {
      "anyOf": [
        {
          "type": "string",
          "pattern": "^P[1-9][0-9]*b?$",
          "maxLength": 32
        },
        {
          "type": "null"
        }
      ]
    },
    "status": {
      "enum": [
        "draft",
        "in_progress",
        "ready_to_publish",
        "published",
        "cancelled"
      ]
    },
    "steps": {
      "type": "array",
      "minItems": 7,
      "maxItems": 7,
      "items": {
        "type": "object",
        "required": [
          "order",
          "key",
          "required",
          "status"
        ],
        "properties": {
          "order": {
            "type": "integer",
            "minimum": 1,
            "maximum": 7
          },
          "key": {
            "enum": [
              "temperature",
              "flow",
              "pressure_advance",
              "retraction",
              "dimensional",
              "overhang",
              "ironing"
            ]
          },
          "required": {
            "type": "boolean"
          },
          "status": {
            "enum": [
              "not_started",
              "in_progress",
              "completed",
              "needs_review",
              "skipped"
            ]
          },
          "result": {}
        }
      }
    }
  }
}
